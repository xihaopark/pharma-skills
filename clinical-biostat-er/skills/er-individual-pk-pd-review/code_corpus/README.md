# Core 2 Code Corpus

This directory provides reference plotting templates for Core 2. Runtime
implementation lives under `../scripts/modules/`, loaded through
`../scripts/er_individual_pk_pd_review_helpers.R`.

Future PK-DQ candidate generation should add new runtime modules first, then
document the public API here.
