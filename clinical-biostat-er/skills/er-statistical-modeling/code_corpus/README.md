# Core 5 Code Corpus

This directory is the reference/API surface for statistical modeling helpers.
Runtime implementation lives under `../scripts/modules/`, loaded through
`../scripts/er_statistical_modeling_helpers.R`.

Unsupported model families remain method-audit entries until explicitly
implemented and evaluated.
