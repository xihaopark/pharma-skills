# Core 4 Code Corpus

This directory is the reference/API surface for ER exploration helpers. Runtime
implementation lives under `../scripts/modules/`, loaded through
`../scripts/er_exposure_response_exploration_helpers.R`.

The corpus documents primitives and composition patterns; it is not a second
runtime implementation.
