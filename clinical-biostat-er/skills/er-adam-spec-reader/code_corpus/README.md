# ADaM Spec Reader Code Corpus

This directory documents the ADaM spec reader API. Runtime implementation lives
under `../scripts/modules/`, loaded through
`../scripts/er_adam_spec_reader_helpers.R`.

The reader emits dictionaries for consuming cores; it does not interpret
question-specific PARAMCD logic.
