# Case 20: Runner Entrypoint Handoff

You are Claude Code validating whether the local `clinical-biostat-er` skill
bundle can still execute its ER workflow scaffold through the default
handoff/validation entrypoint.

Task:

> Verify the skill bundle using the standard validation command that a Codex
> planner should hand off to Claude. Report whether the bundle is currently
> executable, where the validation summary was written, and where the fresh
> scaffold run and comparison pack were written.

Constraints:

- Work from `/Users/park/code/AZ/clinical-biostat-er`.
- Treat `/Users/park/code/AZ/mock_dataset_01_small_molecules_onco` and
  `/Users/park/code/AZ/mock_dataset_02_cart_nononco` as read-only baselines.
- Write generated outputs only under `clinical-biostat-er/evals/_runs/`.
- Do not modify runtime code during this eval.
- Do not use `evals/DEBUG_LOG.md` or prior run summaries as a substitute for a
  fresh validation command.
- Do not call the result final, regulatory-ready, labeling-ready,
  dose-selection-ready, complete, or decision-ready.

Read these files first:

```text
SKILL.md
skills/codex-claude-handoff/SKILL.md
skills/codex-claude-handoff/references/handoff-template.md
evals/agent_behavior/README.md
```

Run the default validation command identified by those files.

Expected answer:

- Files read before execution.
- Exact validation command run.
- Pass/fail result.
- Runner report root.
- Path to `validation_summary.csv`.
- Path to `analyst_execution_summary.md` and
  `analyst_execution_summary_contract.csv`; summarize its required content
  sections: Core 1-6 execution, reproduction coverage, mock02 CAR-T/SLE
  generalization, per-artifact manifest evidence, AZ data defects, review
  gates, and boundary.
- Fresh Case 19 run root generated by the runner.
- Fresh Case 19 source dependency audit path and concise blocked/available
  counts from `source_dependency_audit.csv`.
- Fresh Case 19 Core 6 source-dependency handoff path, concise
  `handoff_status` counts from `source_dependency_handoff.csv`, and any blocked
  required dependency rows such as `model_posthoc_sdtab1062`.
- Fresh Case 19 posthoc sdtab adapter audit path, status counts, and missing
  required columns from `posthoc_sdtab_adapter_audit.csv` when present.
- Fresh Case 19 posthoc exposure-data schema contract path and required-column
  count from `posthoc_exposure_data_schema.csv` when present.
- Fresh Case 19 mock01 Results table manifest path and concise status counts
  from `mock01_results_table_manifest.csv`; when `model_posthoc_sdtab1062` is
  blocked this should show 9 blocked Results-table targets.
- Fresh Case 19 mock01 Core 4 ER pair figure manifest path and concise status
  counts from `mock01_er_pair_figure_manifest.csv`; when posthoc exposure data
  is unavailable this should show 32 blocked ER-pair figure targets.
- Fresh Case 19 mock01 Core 5 KM/Cox/TTE figure manifest path and concise status
  counts from `mock01_km_cox_figure_manifest.csv`; when posthoc exposure data is
  unavailable this should show 16 blocked KM/Cox/TTE figure targets.
- Fresh Case 21 mock02 run root generated by the runner.
- Fresh Case 21 mock02 CAR-T/SLE generalization evidence: Core 1-6 pipeline
  status, scenario key, PKCARTC exposure rows, DORIS W12 evaluable/response
  counts, DORIS x PKCARTC model statuses, and Core 6 open-gate status.
- Latest comparison pack directory generated by the runner.
- Latest comparison pack coverage summary path and concise matched/missing
  counts from `coverage_summary.csv`.
- Latest reference Results target contract path and concise target counts by
  artifact type and owner/gap class from `reference_results_targets.csv`.
- Latest comparison pack missing-artifact backlog path and concise owner-core /
  gap-class counts from `missing_artifact_backlog.csv`.
- Plot capability ownership map path and concise counts from
  `plot_capability_ownership_map.csv`: plot class count, total figure count,
  and whether every row has `runner_may_inline_code = no`. Do not write new
  deliverable plotting implementations inline; call the listed builder-owned
  helper/exporter or report the missing capability/review gate.
- Latest data defect register path and concise defect status / impacted artifact
  counts from `data_defect_register.csv`; explicitly state whether the delivered
  AZ data package is missing an upstream dependency needed for reference Results.
- Latest AZ data follow-up packet path from `az_data_followup_packet.md`; this
  is the human-readable request to send back to AZ if their delivered package is
  missing required upstream data.
- If `missing_artifact_backlog.csv` contains `blocking_status`, report concise
  blocking-status counts and the dependency ids, especially
  `model_posthoc_sdtab1062`.
- Latest Results table reproduction readiness path and concise readiness-status
  counts from `results_table_reproduction_readiness.csv`.
- Any table comparison mismatch statuses from `manifest.csv`:
  `table_schema_mismatch`, `table_row_count_mismatch`, `table_numeric_diff`,
  `table_value_mismatch`, or `table_read_error`.
- Runner step table or concise step status summary.
- Stable analyst-facing summary boundary: the summary is a review/evaluation
  contract, not a final clinical report.
- Clear boundary statement that this proves scaffold/review-package execution,
  not final clinical, regulatory, labeling, dose-selection, or decision-ready
  conclusions.
